# Elasticsearch Basic Auth (placeholders only – DO NOT PUT REAL SECRETS HERE)
ES_ENDPOINT=
ES_USERNAME=
ES_PASSWORD=
# Alternative auth (use instead of username/password when available)
# ES_API_KEY=

# TLS
ES_TLS_REJECT_UNAUTHORIZED=true

# Safety / limits
ES_MAX_SEARCH_SIZE=100
ES_REQUIRE_TIME_RANGE=true
ES_DEFAULT_LOOKBACK=now-15m
ES_ALLOWED_INDEX_PATTERNS=logs-*,metrics-*
ES_VERBOSE=false
